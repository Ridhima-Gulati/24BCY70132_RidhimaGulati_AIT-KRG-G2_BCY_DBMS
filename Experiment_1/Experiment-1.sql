CREATE TABLE BOOKS (

ID INT PRIMARY KEY,
NAME VARCHAR(20) NOT NULL,
AUTHOR_NAME VARCHAR(20) NOT NULL

)

INSERT INTO BOOKS VALUES(1, 'HARRY POTTER', 'JK ROWLING')

ALTER TABLE BOOKS
ADD COUNT INT CHECK(COUNT >=1)

SELECT * FROM BOOKS

UPDATE BOOKS 
SET COUNT=3
WHERE ID=1


CREATE TABLE LIBRARY_VISITOR_USER(

USER_ID INT PRIMARY KEY,
USER_NAME VARCHAR(20),
AGE INT CHECK(AGE>=17) NOT NULL,
EMAIL_ID VARCHAR(20) UNIQUE NOT NULL
)

INSERT INTO LIBRARY_VISITOR_USER VALUES(101, 'Akash Sharma', 19, 'abc@gmail.com')

SELECT * FROM LIBRARY_VISITOR_USER

UPDATE LIBRARY_VISITOR_USER
SET USER_NAME='Sanya'
WHERE USER_ID=102

SELECT * FROM LIBRARY_VISITOR_USER

ALTER TABLE LIBRARY_VISITOR_USER
DROP COLUMN EMAIL_ID

SELECT * FROM LIBRARY_VISITOR_USER

ALTER TABLE LIBRARY_VISITOR_USER
ADD COLUMN EMAIL_ID VARCHAR(30) UNIQUE 

UPDATE LIBRARY_VISITOR_USER
SET EMAIL_ID = 'AK @ GAMIL.COM'
WHERE USER_ID=101

UPDATE LIBRARY_VISITOR_USER
SET EMAIL_ID = 'SANYA @ GAMIL.COM'
WHERE USER_ID=102


CREATE TABLE BOOK_ISSUE(
BOOK_ISSUE_ID INT PRIMARY KEY,
BOOK_ID INT REFERENCES BOOKS(ID),
USER_ID INT REFERENCES LIBRARY_VISITOR_USER(USER_ID)NOT NULL,
BOOK_ISSUE DATE NOT NULL
)

INSERT INTO BOOK_ISSUE VALUES(555, 1, 101, '5-01-2026')

SELECT * FROM BOOK_ISSUE

CREATE ROLE LIBRARIAN
WITH LOGIN PASSWORD 'Ridhima12345'


GRANT SELECT, INSERT, DELETE, UPDATE ON BOOKS TO LIBRARIAN
GRANT SELECT, INSERT, DELETE, UPDATE ON BOOK_ISSUE TO LIBRARIAN
GRANT SELECT, INSERT, DELETE, UPDATE ON LIBRARY_VISITOR_USER TO LIBRARIAN

REVOKE SELECT, INSERT, DELETE, UPDATE ON LIBRARY_VISITOR_USER FROM LIBRARIAN